# Pinned to the Anaconda environment the project was built and validated on
# (Python 3.13). Keep pyarrow at 19.x: parquet written by pyarrow >= 20 can't be
# read by 19 ("Repetition level histogram size mismatch"), and data/ is shared
# between Docker and the local environment.

# data + model
pandas==2.2.3
numpy==2.1.3
pyarrow==19.0.0
scipy==1.15.3
scikit-learn==1.6.1
xgboost==3.1.2
shap==0.51.0
numba==0.61.0
optuna==4.5.0

# scraping (notebooks 01, 02, 05)
requests==2.32.3
beautifulsoup4==4.12.3
rapidfuzz==3.14.3
playwright==1.63.0     # notebook 05's optional AUTO_FETCH; off by default

# app + charts
streamlit==1.45.1
altair==5.5.0
matplotlib==3.10.0

# notebooks
jupyterlab==4.3.4
ipykernel==6.29.5
