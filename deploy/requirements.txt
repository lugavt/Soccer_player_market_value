# What the app needs at runtime — a subset of the root requirements.txt, same
# pins (the pickled models need the exact xgboost/scikit-learn versions).
pandas==2.2.3
numpy==2.1.3
pyarrow==19.0.0
scipy==1.15.3
scikit-learn==1.6.1
xgboost==3.1.2
shap==0.51.0
numba==0.61.0
streamlit==1.45.1
altair==5.5.0
